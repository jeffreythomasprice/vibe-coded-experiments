{
  "name": "@file-manager/cli",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "files": "dist/files"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build --compile src/index.ts --outfile dist/files --target bun",
    "test": "bun test"
  },
  "dependencies": {
    "@file-manager/schemas": "workspace:*",
    "@file-manager/shared": "workspace:*",
    "commander": "^12.1.0"
  }
}
