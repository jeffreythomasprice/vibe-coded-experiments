# Where the client looks for the server. These are baked in at build time
# (see build.rs) since wasm has no runtime environment.
SERVER_HTTP_URL=http://localhost:8001
SERVER_WS_URL=ws://localhost:8001/ws
