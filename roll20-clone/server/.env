# IP address to bind to (127.0.0.1 == localhost). Use 0.0.0.0 to listen on all
# interfaces.
SERVER_HOST=127.0.0.1
SERVER_PORT=8001

# Optional: override log filtering. Defaults to `warn,server=trace,shared=trace`.
# RUST_LOG=warn,server=trace,shared=trace
