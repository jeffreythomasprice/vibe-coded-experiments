[workspace]
resolver = "2"
members = ["shared", "server", "client"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
