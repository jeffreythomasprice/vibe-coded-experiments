active:

todo:

I want the UI to make it easy to roll dice, or to tell me me what dice I should be rolling for various actions.
We should be able to "select" attributes and abilities by clicking on the title. This should bold the name of the ability or attribute. Only one of each can be selected; selecting a 2nd in the same category unselects the first.

There should be a new panel of common dice actions. This should include common things like various combat actions, as well as a roll for the currently selected ability and attribute. The buttons should include any obvious bonuses that we might want to apply, e.g. make it easy to apply an excellency charm if it might apply to our currently selecting ability or attribute. Clicking the buttons should automatically perform this and subtract the required motes. It should be obvious from the button text what roll will be performed (e.g. "Strength 3 + Martial Arts 2 = 5"). This should default to the right panel.

There should be another new panel that is a log of dice actions. If a dice action is clicked and this panel is hidden it should automatically be shown, and made the active tab in whichever location it is shown. This should default to the bottom middle panel. Example logging, "Strength + Martial Arts = 5d6 = 1 + 4 + 4 + 3 + 2 = 14". There should be a button to clear the log, and another to copy the whole log to the clipboard.


In the UI, it would be helpful to have a tooltip on most fields explaining what this is in a 1-3 sentences. Iterate through the whole UI and add tooltips. Generate the text from the rule book as necessary. We can do batches of them in parallel in subagents.


various bits of rules, unclear if these are just notes we should take, or if there are also missing bits of character sheet:
- per-scene or ephemeral combat stuff like: tick clock, onslaught counters, anima level, DV penalty stack
- sorcery/spellcasting mechanics, rules
- limit/virtue/flaw mechanics
- mass combat rules
- social combat rules
- mortal/heroic-mortal variants


embed document-search into this app, pre-chewed rule books
