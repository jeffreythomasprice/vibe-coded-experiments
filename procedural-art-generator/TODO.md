generators should have parameters that we don't iterate through, but are just fixed (e.g. perlin noise texture size)

generators should have the ability to randomize a non-parameterized value, e.g. generate a new random seed or pick one

copy parameters from a single grid cell to clipboard

when dragging sliders around we should show a preview of that value if it can be turned into a color

vector art to mesh that includes texture coordinates

draw vector art to textures
