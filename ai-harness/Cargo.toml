[workspace]
resolver = "2"
members = ["client", "server", "lib", "shared"]

[workspace.package]
version = "0.1.0"
edition = "2021"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
shared = { path = "shared" }
lib = { path = "lib" }
