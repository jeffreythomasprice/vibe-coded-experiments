rename this project

feature wishlist:
- hybrid search: both chunked rag semantic search + plain old keyword search
- small-to-large pattern: tiny chunks but retrieve chunks before and after a match
- chunks are probably about 512 tokens with 50-100 tokens of overlap
- inline citations
- plain text and pdf ingest
- ocr pdf
- audio input, voice to text
- let me select which sources as input, e.g. have pdfs from multiple games but pick which ones matter right now
- organize sources and conversations into projects
- tags? tag conversations and sources and filter by tag instead of individual whitelisting
- re-ranking, once we collect our list of possible chunks for context do some kind of cross-source ranking to unify results from multiple sources
- website search sources, generic scraper
- youtube video downloader
