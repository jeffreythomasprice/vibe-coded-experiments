# diffusion

Generate images with diffusion-rs.

## Build

```
vendor/diffusion-rs-sys/fetch-source.sh   # once, after cloning
cargo build
```

The first build compiles stable-diffusion.cpp and is slow.

## Run

```
cargo run
```