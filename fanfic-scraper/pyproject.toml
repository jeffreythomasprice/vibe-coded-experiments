[project]
name = "fanfic-scraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx",
    "aiosqlite",
    "beautifulsoup4",
    "lxml",
]

[tool.setuptools.packages.find]
where = ["src"]
