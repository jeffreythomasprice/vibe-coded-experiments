config file, where to save files on the backend

black and white option? only outlines?

reference image

generate a parameterized function as output, not a single vector image
like something where we can randomize a few parameters and get very similar images
